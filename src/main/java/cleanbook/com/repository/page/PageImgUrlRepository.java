package cleanbook.com.repository.page;

import cleanbook.com.entity.page.PageImgUrl;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PageImgUrlRepository extends JpaRepository<PageImgUrl,Long> {
}
